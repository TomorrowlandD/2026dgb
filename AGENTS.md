# AGENTS.md

## 项目工作约束

本项目目标是基于 `docs/revised_modeling_plan.md` 完成 B 题《嵌入式社区养老服务站的建设与优化问题》的数据处理、模型求解、结果图表和完整论文撰写。

禁止批量删除文件或目录。

不要使用：

- `del /s`
- `rd /s`
- `rmdir /s`
- `Remove-Item -Recurse`
- `rm -rf`

需要删除文件时，只能一次删除一个明确路径的文件，例如：

```powershell
Remove-Item "C:\path\to\file.txt"
```

如果需要批量删除文件，应停止操作，并请求用户手动删除。

## 当前状态

目前已经完成：

- 题面与附件数据初步理解；
- 主建模方案确定；
- 阶段 0：方案冻结与任务清单确认，检查记录见 `outputs/logs/stage0_check.md`；
- 阶段 1：数据清洗与参数表构建，检查记录见 `outputs/logs/stage1_data_check.md`；
- 阶段 2：问题 1 求解，已完成老人数量预测、理论需求、消费约束后需求和预算检查，检查记录见 `outputs/logs/stage2_problem1_check.md`；
- 阶段 3：问题 2 求解，已完成服务站选址、规模优化、覆盖率与满意度计算，检查记录见 `outputs/logs/stage3_problem2_check.md`；
- 阶段 4：问题 3 求解，已完成补贴导向定价、利润率核算和三类老人可及性评价，检查记录见 `outputs/logs/stage4_problem3_check.md`；
- 阶段 5：问题 4 灵敏度分析，已完成 S0-S4 情景完整重求解、方案变化度和鲁棒性汇总，检查记录见 `outputs/logs/stage5_problem4_check.md`；
- 阶段 0-5 已完成跨阶段复核，检查记录见 `outputs/logs/stage0_5_recheck.md`；
- 阶段 6：结果图表生成，已完成 7 张核心图、图表索引和阶段 6 总检查，检查记录见 `outputs/logs/stage6_6_final_check.md`；
- 五个关键口径统一：
  - 消费约束主模型采用等比例削减；
  - 紧急救助始终公益免费，不参与消费约束、不计补贴，但计入直接成本和服务能力占用；
  - 利润率硬约束只保留 `rho <= 8%`；
  - 问题 2 中价格满意度取 `S3=1`；
  - 问题 4 所有情景均重新完整求解；
- 问题 3 已明确候选价格必须反馈到 `S3`、小区分配、有效服务人次、补贴和利润率。

当前尚未完成：

- 完整论文正文；
- 附录与最终校验。

当前写作辅助材料：

- 最终严谨性修订说明：`docs/final_revision_notes.md`；
- 最终一致性审查：`docs/final_consistency_audit.md`；
- 论文关键结果摘要：`docs/paper_key_results_summary.md`；
- 论文撰写指南：`docs/paper_writing_guide.md`。

## 总体路线

从当前状态到完整论文，按以下阶段推进：

```text
阶段 0：方案冻结与任务清单确认（已完成）
阶段 1：数据清洗与参数表构建（已完成）
阶段 2：问题 1 求解（已完成）
阶段 3：问题 2 求解（已完成）
阶段 4：问题 3 求解（已完成）
阶段 5：问题 4 灵敏度分析（已完成）
阶段 6：结果图表生成（已完成）
阶段 7：论文正文撰写
阶段 8：附录整理与复现说明
阶段 9：全文一致性检查与定稿
```

每个阶段完成后必须执行对应检查。检查未通过时，不进入下一阶段。

## 最终严谨性修订：需要重写哪些部分与修改步骤

### 修订目标

本节用于指导后续 Codex 或人工修改工作。当前不需要推倒重写代码主流程，因为阶段 3、阶段 4、阶段 5 的实现已经基本符合最终严谨口径：

- 问题 2 排序已采用 `CR_srv -> CR_eff -> weighted_satisfaction -> DR -> 建设成本低`；
- 问题 3 已对每组候选价格重新计算 `S3`、小区分配、有效服务人次、利用率、补贴和利润率；
- 问题 4 已对 S0-S4 进行完整重求解。

后续重点是重写或修订文档、论文解释和少量检查表述，确保“可执行、口径统一、能写进论文”。

### 必须重写或修订的内容

#### A. 必须重写：建模方案文档中的口径说明

目标文件：

- `docs/revised_modeling_plan.md`

必须修订：

1. 问题 1 消费约束：
   - 主模型写成“收费服务等比例削减”；
   - 紧急救助写成公益免费，不参与消费约束、不计补贴，但计入直接成本和服务能力占用；
   - “刚需优先”只能写成补充对比、局限性或改进方向，不能写成核心创新点；
   - 增加取整规则：模型内部连续计算，需求展示四舍五入，金额、满意度、补贴、利润等不提前取整。

2. 问题 2 目标函数：
   - 排序目标固定为：
     ```text
     max CR_srv
     -> max CR_eff
     -> max weighted_satisfaction
     -> max DR
     -> min construction_cost 或 min station_count
     ```
   - 不把年化利润作为问题 2 主排序目标；
   - 年度利润只作为问题 2.3 中“基准价格下的测算指标”报告；
   - 政府补贴和最终定价后的利润分析放入问题 3。

3. 问题 3 定价反馈闭环：
   - 明确每组候选价格都必须重新计算：
     ```text
     p_k
     -> S3
     -> S_ij = 0.2*S1 + 0.3*S2 + 0.5*S3
     -> 小区分配
     -> E_pre_{i,r,k}
     -> gamma_j
     -> E_{i,r,k}
     -> utilization_j 和 S2_j
     -> G_j、R_j、VC_j、rho_j、Net_j
     -> 可及性
     ```
   - 主模型采用统一服务价格 `p_k`；
   - 每站每服务独立浮动 `delta_{j,k}` 不进入主模型，只能作为扩展模型或改进方向；
   - 如需站点差异，可最多采用“每个站点一个整体浮动比例 `delta_j`”作为可选对照。

4. 利润率和年度净收益：
   - 题目利润率只采用题目口径：
     ```text
     ServiceProfit_j = R_j - VC_j
     OC_j = 365 * F_j
     rho_topic_j = (ServiceProfit_j + G_j - OC_j) / (OC_j + epsilon)
     ```
   - 年度净收益单独定义：
     ```text
     A_j = 10000 * B_j / 20
     Net_j = R_j + G_j - VC_j - OC_j - A_j
     ```
   - 删除或改写“利润率采用总成本口径”的残留表述；
   - 最终采用方案 A：硬约束为 `rho_topic <= 8%`，不硬性要求 `rho_topic >= 0`；
   - 若某站点亏损，只能表述为“存在保本风险，需要政府统筹或财政兜底”，不能写成财务完全可持续。

5. 类型层面的有效服务量：
   - 补充定义：
     ```text
     E_pre_{i,r,k} = D_{i,r,k} * S_{i,j(i)}
     E_{i,r,k} = gamma_{j(i)} * E_pre_{i,r,k}
     E_{i,k} = sum_r E_{i,r,k}
     ```
   - 该定义必须用于三类老人经济、地理、服务满足和综合可及性评价。

6. 服务覆盖率近似说明：
   - `CR_srv` 使用 `sum_k E_{i,k} > theta` 是小区级聚合近似；
   - 论文中必须解释：题目未提供个体服务获得记录，因此以小区为基本评价单元；
   - 同时报告 `CR_eff` 和 `DR` 作为辅助指标，弥补粗粒度覆盖率的不足。

7. 问题 4 灵敏度分析：
   - 明确 S0-S4 每个情景都重新执行问题 1、问题 2、问题 3；
   - 不能只替换最终指标；
   - 必须比较站点数量、位置、规模、服务定价、补贴总额、覆盖率、满意度、题目利润率和年度净收益；
   - 对 140 万预算情景，不能简单写“预算增加一定更好”。如果扩张方案财务风险上升，应解释为“预算提高诱导扩张，但运营可持续性需要再筛选”。

#### B. 只需复查，不建议重写：代码主流程

目标文件：

- `src/run_stage2_problem1.py`
- `src/run_stage3_problem2.py`
- `src/run_stage4_problem3.py`
- `src/run_stage5_problem4.py`

当前原则：

- 不重写主流程；
- 不新增复杂模型；
- 只在发现与最终口径不一致时做最小修正；
- 修正后必须重新运行对应阶段及后续依赖阶段。

复查重点：

1. `run_stage2_problem1.py`
   - 收费服务是否等比例削减；
   - 紧急救助是否保持理论需求；
   - 展示取整是否不影响内部计算。

2. `run_stage3_problem2.py`
   - 排序是否未使用年化利润；
   - `E_{i,r,k}` 是否保留并输出；
   - `CR_srv` 是否按小区聚合阈值计算。

3. `run_stage4_problem3.py`
   - 每组价格是否重新计算 `S3`；
   - 是否重新分配小区；
   - 是否重新计算有效服务量、利用率、补贴、利润率；
   - 是否未强制 `rho >= 0`；
   - 是否用最终 `E_{i,r,k}` 计算老人支付额和可及性。

4. `run_stage5_problem4.py`
   - S0-S4 是否完整重求解；
   - 是否输出年度净收益和利润率；
   - 是否对 S3 成本变化、S4 预算变化给出解释性检查。

#### C. 必须重写：论文正文相关解释

目标阶段：

- 阶段 7：论文正文撰写
- 阶段 8：附录整理
- 阶段 9：全文一致性检查

必须重写或特别注意：

1. 摘要：
   - 不写“刚需优先”为主模型；
   - 写“等比例消费约束 + 紧急救助公益剥离”；
   - 不把亏损方案写成完全可持续。

2. 模型假设：
   - 明确紧急救助公益免费；
   - 明确小区级覆盖率近似；
   - 明确内部连续计算、结果展示取整。

3. 问题 2 结果分析：
   - 只按覆盖、满意度、需求满足和建设成本解释选址；
   - 年度利润只能作为基准价格测算，不作为选址依据。

4. 问题 3 结果分析：
   - 明确定价会反馈到满意度、分配、有效服务人次和财务指标；
   - 分开解释题目利润率和年度净收益；
   - 若存在负利润率或负净收益，必须写“存在保本风险”。

5. 问题 4 结果分析：
   - 所有情景写成完整重求解；
   - 预算提高情景要同时讨论覆盖收益和财务风险；
   - 若某情景扩张但净收益恶化，不得简单称为“更优”。

### 推荐修改步骤

按以下顺序执行，不要跳步：

1. **修订方案文档**
   - 修改 `docs/revised_modeling_plan.md`；
   - 完成上述 A 类所有口径修订；
   - 用文本搜索确认没有旧表述残留：
     - `刚需优先` 是否只出现在补充对比/改进方向；
     - `年化利润` 是否不再作为问题 2 排序目标；
     - `利润率采用总成本口径` 是否已删除；
     - `0 <= rho <= 8%` 是否未作为硬约束出现。

2. **复查代码是否需要最小修正**
   - 阅读 `src/run_stage2_problem1.py` 至 `src/run_stage5_problem4.py`；
   - 若代码已符合最终口径，不修改代码；
   - 若发现不一致，只改对应阶段脚本，并重新运行该阶段及后续阶段。

3. **重新生成检查日志**
   - 如果代码未改，只补充最终严谨性审查日志；
   - 如果代码改动，重新运行受影响阶段，更新 `outputs/logs/`。

4. **复查结果表和图表是否仍一致**
   - 检查 `outputs/tables/problem3_station_finance.csv`；
   - 检查 `outputs/tables/problem4_scenario_metrics.csv`；
   - 若数值变化，重新运行阶段 6 图表脚本；
   - 若数值未变，不重画图。

5. **进入论文正文撰写**
   - 先写模型建立与求解；
   - 再写结果分析；
   - 最后写摘要；
   - 写作时严格使用本节最终口径。

### 不需要重写的内容

除非检查发现错误，否则不要重写：

- 数据清洗逻辑；
- 问题 1 老人数量递推；
- 问题 2 枚举剪枝框架；
- 问题 3 粗网格/细网格搜索框架；
- 问题 4 情景重求解框架；
- 已生成的 7 张核心图。

### 修改完成后的验收标准

- `docs/revised_modeling_plan.md`、代码检查日志、论文正文三者口径一致；
- 主模型中不再把“刚需优先”写成核心创新点；
- 问题 2 不再用年化利润排序；
- 问题 3 定价闭环解释完整；
- 利润率和年度净收益完全分开；
- `rho <= 8%` 是硬约束，`rho >= 0` 不是硬约束；
- 亏损风险在结果分析中被明确说明；
- 问题 4 每个情景均写成完整重求解。

## 阶段 0：方案冻结与任务清单确认

状态：已完成。检查记录：`outputs/logs/stage0_check.md`。

### 目标

确认 `docs/revised_modeling_plan.md` 是后续代码、图表和论文写作的唯一主方案。

### 执行任务

- 阅读 `docs/revised_modeling_plan.md`。
- 确认主模型口径：
  - 问题 1.3 使用等比例消费约束；
  - 刚需优先只做补充对比；
  - 问题 2 选址阶段取 `S3=1`；
  - 问题 3 每组候选价格都重新计算价格满意度和有效服务人次；
  - 问题 4 所有情景完整重求解。
- 将后续代码输出目录统一放在 `outputs/` 下。

### 产出

- 明确的实施任务清单；
- 确认不再随意更改主模型口径。

### 检查

- [x] 文档中没有“刚需优先作为主模型”的残留表述。
- [x] 文档中没有 `0 <= rho <= 8%` 作为硬约束的残留表述。
- [x] 问题 3 已明确价格反馈到满意度、分配、有效服务量和利润率。
- [x] 问题 2 年度利润只作为选址后测算，不作为选址主目标。

## 阶段 1：数据清洗与参数表构建

状态：已完成。检查记录：`outputs/logs/stage1_data_check.md`。核心脚本：`src/data_loader.py`。清洗后参数表位于 `outputs/tables/stage1_*.csv` 和 `outputs/tables/stage1_cleaned_parameters.json`。

### 目标

将 `data/` 中的五个 Excel 附件整理为程序可直接调用的标准参数。

### 执行任务

- 读取附件 1：小区基础数据和转移概率。
- 读取附件 2：服务需求次数、基准价格、直接支出、消费上限。
- 读取附件 3：建设成本、日固定管理成本、日最大服务人次。
- 读取附件 4：小区间距离矩阵。
- 读取附件 5：满意度评分规则。
- 统一命名：
  - “半自理”统一为“半失能”；
  - 服务名称统一为：助餐、日间照料、上门护理、康复理疗、助浴、紧急救助。
- 统一单位：
  - 建设成本以万元用于建设预算，以元用于年度成本核算；
  - 距离统一为米；
  - 百分比统一转为小数。

### 产出

- 清洗后的参数对象或中间表；
- 数据检查日志；
- 可复用的数据读取函数。

### 检查

- [x] 10 个小区编号完整且顺序一致。
- [x] 三类老人初始人数加总等于 60+ 老人数。
- [x] 距离矩阵为 10 x 10，主对角线为 0，矩阵对称。
- [x] 服务项目为 6 项，其中紧急救助价格为 0。
- [x] 消费上限比例正确转为 `0.20`、`0.25`、`0.30`。
- [x] 建设成本、运营成本、容量均非负。
- [x] 无异常空值进入模型计算。

## 阶段 2：问题 1 求解

状态：已完成。检查记录见 `outputs/logs/stage2_problem1_check.md`。

### 目标

预测未来五年老人数量，并计算第 5 年末理论需求和消费约束后需求。

### 执行任务

- 实现三状态老人数量递推。
- 按年输出第 1 至第 5 年各小区三类老人数量。
- 根据服务需求频次计算第 5 年理论月需求。
- 对收费服务按等比例消费约束修正需求。
- 紧急救助不参与消费约束削减。
- 计算需求满足率和预算使用率。
- 计算过程中保留小数，最终展示表按题目要求取整。

### 产出

- `outputs/tables/problem1_population_forecast.*`
- `outputs/tables/problem1_theoretical_demand.*`
- `outputs/tables/problem1_actual_demand.*`
- `outputs/tables/problem1_budget_check.*`

### 检查

- [x] 新增老人只进入自理老人状态。
- [x] 新增项未乘死亡率。
- [x] 老人数量均非负。
- [x] 实际需求不超过理论需求。
- [x] 紧急救助实际需求等于理论需求。
- [x] 收费服务在预算不足时按同一比例削减。
- [x] 预算不足时预算使用率接近 1。
- [x] 输出展示值已按要求取整，模型内部计算值未提前取整。

## 阶段 3：问题 2 求解

### 目标

在 120 万元建设预算和 1000 米服务半径约束下，确定服务站数量、位置和规模。

### 执行任务

- 枚举 10 个小区的 4 种状态：不建站、小型、中型、大型。
- 排除空方案。
- 进行建设预算剪枝。
- 对每个可行方案执行满意度-容量-利用率-分配迭代。
- 问题 2 中价格满意度固定取 `S3=1`。
- 计算 `CR_srv`、`CR_geo`、`CR_eff`、`DR`、加权满意度、利用率、容量可得系数。
- 按分层目标排序：
  - 最大化 `CR_srv`；
  - 最大化 `CR_eff`；
  - 最大化加权满意度；
  - 最大化 `DR`；
  - 并列时选择建设成本更低。
- 对最优方案用基准价格测算题目 2.3 所需预计年度利润。

### 产出

- `outputs/tables/problem2_best_station_plan.*`
- `outputs/tables/problem2_assignment.*`
- `outputs/tables/problem2_station_utilization.*`
- `outputs/tables/problem2_coverage_summary.*`
- `outputs/tables/problem2_top_candidates.*`

### 检查

- [x] 枚举方案总数为 `4^10`，并记录剪枝数量。
- [x] 所有选中站点建设成本总额不超过 120 万元。
- [x] 每个被分配小区与服务站距离不超过 1000 米。
- [x] 未覆盖小区的有效服务人次为 0。
- [x] `gamma_j` 基于容量调整前的 `E_pre` 计算。
- [x] 保留 `E_{i,r,k}`，用于后续三类老人可及性。
- [x] 迭代收敛或记录未收敛原因。
- [x] 预计年度利润与问题 3 优化定价结果明确区分。

## 阶段 4：问题 3 求解

### 目标

在问题 2 最优站点方案基础上，求解服务定价、政府补贴、利润率和三类老人可及性。

### 执行任务

- 固定问题 2 的站点位置和规模。
- 生成非紧急救助服务的候选价格。
- 紧急救助价格固定为 0。
- 先做统一指导价粗搜索，再做细搜索。
- 主实现采用站点整体浮动 `delta_j`，避免 `delta_{j,k}` 组合爆炸。
- 对每组候选价格：
  - 重新计算价格满意度 `S3`；
  - 重新计算综合满意度；
  - 重新执行满意度-容量-利用率-分配迭代；
  - 重新计算 `E_{i,r,k}`、收入、直接成本、补贴、利润率、年度净收益；
  - 检查 `rho <= 8%`。
- 按满意度、亏损惩罚、老人支付额、政府补贴压力进行排序。

### 产出

- `outputs/tables/problem3_prices.*`
- `outputs/tables/problem3_station_finance.*`
- `outputs/tables/problem3_price_satisfaction.*`
- `outputs/tables/problem3_accessibility.*`

### 检查

- [x] 每组候选价格都重新计算 `S3`。
- [x] 每组候选价格都重新计算分配结果和有效服务人次。
- [x] 紧急救助不计政府补贴，但计入直接服务成本。
- [x] 政府补贴不超过每日补贴上限折算值。
- [x] 题目口径利润率采用 `(ServiceProfit + G - OC) / OC`。
- [x] 年度净收益另行计入年化建设成本。
- [x] 所有入选方案满足 `rho <= 8%`。
- [x] 没有将 `rho >= 0` 作为硬约束。
- [x] 价格满意度不超过 1。
- [x] 老人支付额使用最终有效服务人次 `E_{i,r,k}` 计算。

## 阶段 5：问题 4 灵敏度分析

状态：已完成。检查记录见 `outputs/logs/stage5_problem4_check.md`。

### 目标

对题目规定参数变化进行完整重求解，并比较方案稳定性。

### 执行任务

- 设置基准情景 `S0`。
- 设置老人增长率调整为 8% 的情景。
- 设置转移概率调整情景：自理到半失能为 5.5%，半失能到失能为 9.5%。
- 设置日固定管理成本增加 20% 的情景。
- 设置建设预算调整为 140 万元的情景。
- 每个情景均重新求解受影响的需求、选址、定价、补贴、利润率和可及性。
- 计算站点位置变化率、规模变化率、覆盖率变化率、满意度变化率、利润变化率、补贴变化率和综合变化度。

### 产出

- `outputs/tables/problem4_scenario_station_plans.*`
- `outputs/tables/problem4_scenario_metrics.*`
- `outputs/tables/problem4_variation_indices.*`
- `outputs/tables/problem4_robustness_summary.*`

### 检查

- [x] 每个情景均重新完整求解，未只替换最终指标。
- [x] 每个情景均输出站点数量、位置和规模。
- [x] 每个情景均输出定价、补贴、覆盖率、满意度、利润率。
- [x] 成本增加情景若选址不变，已解释原因。
- [x] 预算调整情景使用 140 万元建设预算。
- [x] 方案变化度指标公式与文档一致。

## 阶段 6：结果图表生成

### 目标

将阶段 1-5 的数值结果分批转化为论文可直接使用的表格和图形。阶段 6 不一次性生成所有图片，而是按论文叙事顺序逐步完成，每完成一组图都必须检查后再进入下一组。

所有带数值、坐标轴、指标和结论的图都使用 Python 绘制，优先使用 `pandas`、`matplotlib`、`seaborn`。不要用大模型生成数据图。大模型生图只允许用于非数据型概念插图，且默认不进入数学建模论文主体。

### 总体原则

- 先确定图表清单，再开始绘图。
- 每张图必须对应一张结果表或一个明确结果指标。
- 每张图必须服务论文中的一个具体结论。
- 每次只生成一组图，检查通过后再继续。
- 图不求多，优先保证可复核、清晰、与正文结论一致。
- 表格能讲清楚的内容不要强行画图。

### 执行任务

阶段 6 拆分为以下子步骤。

### 阶段 6.0：图表计划确认

#### 目标

确定本轮论文需要哪些图，避免盲目生成大量图片。

#### 执行任务

- 汇总阶段 1-5 已生成的结果表。
- 为每张候选图填写图表计划：
  - 图编号；
  - 所属问题；
  - 对应输入表；
  - 图形类型；
  - 支撑的论文结论；
  - 是否必须生成。
- 将图分为“核心图”和“可选图”。

#### 建议核心图

当前阶段只计划生成以下 7 张核心图。除非论文写作阶段发现证据不足，不主动生成备用图。

- 图 1：三类老人五年预测趋势图。
- 图 2：第 5 年各小区服务需求堆叠柱状图。
- 图 3：最优服务站-小区分配示意图。
- 图 4：各服务站利用率与容量可得系数图。
- 图 5：优化价格与基准价格对比图。
- 图 6：三类老人可及性对比图。
- 图 7：灵敏度情景主要指标对比图。

#### 备用图表方案

以下图表作为备用方案记录在文档中，当前不生成。只有当论文正文需要补充证据、核心图解释力度不足，或用户明确要求补图时，才从该清单中选择生成。

##### 正文补充图

这些图可以进入正文，但优先级低于 7 张核心图。

| 备用编号 | 图表名称 | 对应问题 | 用途 | 触发条件 |
|---|---|---|---|---|
| 图 8 | 消费约束前后需求对比图 | 问题 1 | 展示等比例消费约束对需求总量的影响 | 需要解释消费约束修正效果时生成 |
| 图 9 | 各服务项目需求满足率热力图 | 问题 1 / 问题 2 | 展示不同小区、不同服务的满足差异 | 需要说明哪些服务存在短板时生成 |
| 图 10 | 覆盖率、满意度、需求满足率汇总图 | 问题 2 | 汇总展示选址方案综合表现 | 结果分析中指标较多、表格不直观时生成 |
| 图 11 | 服务站补贴与利润率对比图 | 问题 3 | 展示补贴、利润率和经营可持续性 | 需要强调补贴和利润率约束时生成 |
| 图 12 | 方案综合变化度柱状图 | 问题 4 | 展示各情景下方案稳定性 | 需要突出鲁棒性结论时生成 |

##### 附录备用图

这些图默认放入附录，不进入正文主体，除非论文篇幅允许且结论确实需要。

| 备用编号 | 图表名称 | 对应问题 | 用途 | 触发条件 |
|---|---|---|---|---|
| 图 A1 | 各小区第 5 年三类老人结构堆叠图 | 问题 1 | 展示不同小区老人类型结构 | 需要补充人口结构差异时生成 |
| 图 A2 | 理论需求与实际需求分服务项目对比图 | 问题 1 | 展示消费约束前后的服务结构变化 | 需要展开解释需求修正细节时生成 |
| 图 A3 | 候选方案前 3-5 名指标对比图 | 问题 2 | 展示最优方案选择依据 | 需要说明方案优选过程时生成 |
| 图 A4 | 各小区价格满意度热力图 | 问题 3 | 展示定价后不同小区价格满意度差异 | 需要补充价格满意度空间差异时生成 |
| 图 A5 | 小区综合可及性热力图 | 问题 3 | 展示不同小区服务获得程度差异 | 需要定位服务薄弱小区时生成 |
| 图 A6 | 刚需优先补充对比图 | 补充分析 | 对比等比例削减和刚需优先规则 | 需要讨论主模型需求修正规则局限性时生成 |

##### 备用图使用规则

- 当前不生成备用图。
- 备用图必须从已有结果表或可复现中间结果生成。
- 备用图生成前必须先说明其服务的论文结论。
- 若备用图与核心图表达同一结论，优先保留核心图。
- 备用图编号不得占用核心图 `图 1` 到 `图 7`。
- 备用图若进入正文，需要同步更新 `outputs/figures/figure_index.md`。

#### 产出

- `outputs/figures/figure_plan.md`

#### 检查

- [x] 每张候选图都有对应输入表。
- [x] 每张核心图都能支撑一个明确结论。
- [x] 没有把同一结论重复画成多张相似图。
- [x] 图表计划中区分了核心图和可选图。
- [x] 当前生成范围限定为 7 张核心图，备用图仅记录、不执行。

### 阶段 6.1：问题 1 图表生成

#### 目标

用图展示老人数量预测和服务需求结构，为后续选址与定价提供需求背景。

#### 执行任务

- 先整理问题 1 的论文表格：
  - 五年老人数量预测表；
  - 第 5 年理论需求表；
  - 第 5 年消费约束后实际需求表；
  - 预算使用率和需求满足率检查表。
- 生成图 1：三类老人五年预测趋势图。
- 生成图 2：第 5 年各小区服务需求堆叠柱状图。
- 如有必要，再生成消费约束前后需求对比图，作为可选图。

#### 产出

- `outputs/tables/paper_problem1_*.csv` 或 `.xlsx`
- `outputs/figures/fig1_population_forecast.*`
- `outputs/figures/fig2_community_demand_stack.*`
- 可选：`outputs/figures/fig_optional_budget_constraint_compare.*`

#### 检查

- [x] 图 1 数据来自问题 1 老人数量预测表。
- [x] 图 2 数据来自消费约束后的实际需求表。
- [x] 图 1 横轴为年份，纵轴为人数，图例为老人类型。
- [x] 图 2 横轴为小区，纵轴为月服务需求次数，图例为服务项目。
- [x] 图中服务需求展示值与论文表格取整规则一致。
- [x] 图形能支持“需求规模和结构”的文字结论。
- [x] 图 1 已添加各点取整人数标注：蓝线标注在线段下方，绿线标注在线段上方，红线标注在线段上方并略向下调整且不与绿线标注重合，图内不保留顶部标题。
- [x] 图 1 的 PNG 与 SVG 输出已同步更新，并完成版面检查。

### 阶段 6.2：问题 2 图表生成

#### 目标

展示最优服务站建设方案、覆盖关系和服务能力利用情况。

#### 执行任务

- 先整理问题 2 的论文表格：
  - 最优站点位置与规模表；
  - 小区-服务站分配表；
  - 站点利用率与容量可得系数表；
  - 覆盖率、满意度、需求满足率汇总表。
- 生成图 3：最优服务站-小区分配示意图。
- 生成图 4：各服务站利用率与容量可得系数图。
- 如有必要，再生成覆盖率、满意度、需求满足率汇总图，作为可选图。

#### 绘图注意

- 若没有真实地理坐标，不要伪装成真实地图。
- 分配示意图应称为“网络分配示意图”或“服务关系示意图”。
- 可用距离矩阵构造简化布局，但图注中应说明是示意图。

#### 产出

- `outputs/tables/paper_problem2_*.csv` 或 `.xlsx`
- `outputs/figures/fig3_station_assignment.*`
- `outputs/figures/fig4_station_utilization_capacity.*`
- 可选：`outputs/figures/fig_optional_coverage_satisfaction_summary.*`

#### 检查

- [x] 图 3 中所有分配关系满足 1000 米服务半径。
- [x] 图 3 明确区分服务站小/中/大型规模。
- [x] 图 3 未被描述为真实地图，除非有真实坐标。
- [x] 图 3 右侧 `D` 站分配线的视觉长度顺序与标注距离一致：`H(300m) < J(400m) < B(500m) < A(900m)`，且图内不保留顶部标题。
- [x] 图 4 的利用率和容量可得系数来自最终迭代结果。
- [x] 图 4 中容量可得系数位于 `[0,1]`。
- [x] 图形能支持“选址合理性和容量配置”的文字结论。

### 阶段 6.3：问题 3 图表生成

#### 目标

展示优化定价、补贴机制、服务站财务结果和三类老人可及性。

#### 执行任务

- 先整理问题 3 的论文表格：
  - 服务项目优化定价表；
  - 各服务站收入、直接成本、补贴、利润率、年度净收益表；
  - 小区价格满意度表；
  - 三类老人可及性评价表。
- 生成图 5：优化价格与基准价格对比图。
- 生成图 6：三类老人可及性对比图。
- 如有必要，再生成各服务站补贴、利润率或年度净收益图，作为可选图。

#### 绘图注意

- 定价图必须明确区分“基准价格”和“优化价格”。
- 若展示财务图，必须区分题目口径利润率和年度净收益。
- 可及性图优先使用分组柱状图；雷达图只在信息不拥挤时使用。

#### 产出

- `outputs/tables/paper_problem3_*.csv` 或 `.xlsx`
- `outputs/figures/fig5_price_vs_baseline.*`
- `outputs/figures/fig6_accessibility_by_elder_type.*`
- 可选：`outputs/figures/fig_optional_subsidy_profit.*`

#### 检查

- [x] 图 5 的优化价格来自问题 3 最优候选方案。
- [x] 图 5 未混用问题 2 基准价格测算结果。
- [x] 图 6 包含经济、地理、服务满足和综合可及性中的关键指标。
- [x] 可及性指标使用最终有效服务人次 `E_{i,r,k}`。
- [x] 利润率图若生成，必须使用题目口径 `rho <= 8%`。
- [x] 图形能支持“补贴定价降低负担并保持服务可持续性”的文字结论。

### 阶段 6.4：问题 4 图表生成

#### 目标

展示不同情景下方案变化，评价模型敏感性和鲁棒性。

#### 执行任务

- 先整理问题 4 的论文表格：
  - 不同情景建站方案对比表；
  - 不同情景主要指标对比表；
  - 方案变化度指标表。
- 生成图 7：灵敏度情景主要指标对比图。
- 如有必要，再生成方案综合变化度柱状图，作为可选图。
- 如果图 7 信息过多，可以拆成两张：
  - 覆盖率与满意度变化图；
  - 补贴与利润变化图。

#### 产出

- `outputs/tables/paper_problem4_*.csv` 或 `.xlsx`
- `outputs/figures/fig7_scenario_metrics_compare.*`
- 可选：`outputs/figures/fig_optional_scenario_variation_index.*`

#### 检查

- [x] 每个情景的图中数据来自完整重求解结果。
- [x] 图中情景标签与题目情景一致。
- [x] 覆盖率、满意度、补贴、利润率单位清楚。
- [x] 若成本增加情景选址不变，图注或正文中有解释。
- [x] 图形能支持“模型鲁棒性和敏感参数”的文字结论。

### 阶段 6.5：图表编号、索引和统一风格

#### 目标

统一图表编号、文件名、视觉风格和论文引用方式。

#### 执行任务

- 统一所有核心图编号。
- 统一图像文件命名。
- 统一字体、颜色、图例位置、尺寸和分辨率。
- 建立图表索引清单。
- 标注每张图对应的论文段落。

#### 产出

- `outputs/figures/figure_index.md`
- 最终核心图文件：
  - `fig1_*`
  - `fig2_*`
  - `fig3_*`
  - `fig4_*`
  - `fig5_*`
  - `fig6_*`
  - `fig7_*`

#### 检查

- [x] 图表编号和论文正文引用一致。
- [x] 所有图都有标题、坐标轴、单位、图例或必要注释。
- [x] 颜色区分清楚，黑白打印时仍基本可读。
- [x] 图片分辨率满足论文插图要求。
- [x] 文件名能看出图的内容和编号。

### 阶段 6.6：阶段 6 总检查

#### 目标

确认图表能够支撑论文写作，可以进入阶段 7。

#### 执行任务

- 对照题目 1-4 检查每个问题是否有图表支撑。
- 对照 `outputs/tables/` 检查每张图的数据来源。
- 对照论文结论草稿检查每张图的用途。
- 删除计划中未使用的可选图引用，但不要批量删除文件。

### 产出

- `outputs/tables/` 下的论文表格；
- `outputs/figures/` 下的论文图形；
- 图表索引清单。

### 检查

- [x] 每个题目小问至少有一张表或一组指标直接回答。
- [x] 图表单位明确。
- [x] 图表编号和论文结构一致。
- [x] 图表中的数值与模型输出一致。
- [x] 覆盖率、满意度、利润率、补贴金额口径一致。
- [x] 没有把基准价格测算结果和优化定价结果混用。
- [x] 每张核心图都能在论文中找到对应解释段落。
- [x] 阶段 6 的所有检查通过后，再进入阶段 7。

## 阶段 7：论文正文撰写

### 目标

基于模型方案和结果图表撰写完整论文正文。

### 执行任务

推荐写作顺序：

```text
模型建立与求解
→ 结果分析
→ 灵敏度分析
→ 模型评价与改进
→ 问题重述
→ 问题分析
→ 摘要与关键词
```

论文结构采用：

```text
1 问题重述
2 问题分析
3 模型假设
4 符号说明
5 模型建立与求解
6 结果分析
7 灵敏度分析
8 模型评价与改进
9 参考文献
10 附录
```

### 产出

- 论文初稿；
- 摘要；
- 关键词；
- 结果分析文字；
- 模型评价与改进。

### 检查

- [ ] 每个题目小问均被明确回答。
- [ ] 正文公式与 `docs/revised_modeling_plan.md` 一致。
- [ ] 正文结果与 `outputs/tables/` 一致。
- [ ] 图表编号、标题、单位完整。
- [ ] 摘要包含方法、结果和结论。
- [ ] 模型优点、不足和改进方向具体，不空泛。

## 阶段 8：附录整理与复现说明

### 目标

整理代码、算法流程和附录材料，使结果可复核。

### 执行任务

- 整理核心代码文件说明。
- 写明运行顺序。
- 整理参数表、候选方案表和补充对比结果。
- 放入刚需优先补充对比分析。
- 放入必要的检查日志。

### 产出

- 论文附录；
- 复现说明；
- 代码文件索引；
- 参数与结果文件索引。

### 检查

- [ ] 附录中的算法步骤能对应代码。
- [ ] 附录没有放入与主模型冲突的旧口径。
- [ ] 刚需优先明确为补充对比，不写成主模型。
- [ ] 复现说明包含数据输入、运行命令、输出位置。
- [ ] 关键参数均可追溯到附件或建模假设。

## 阶段 9：全文一致性检查与定稿

### 目标

完成最终论文前的统一检查，避免口径冲突、单位错误和结果不一致。

### 执行任务

- 检查题目要求逐项回应情况。
- 检查公式、变量、表格、图形和文字描述是否一致。
- 检查所有单位。
- 检查所有约束是否在结果中满足。
- 检查利润率和年度净收益是否分开表述。
- 检查问题 4 是否完整重求解。
- 压缩冗余文字，增强结果解释。

### 产出

- 完整论文定稿；
- 最终图表；
- 最终代码和复现说明。

### 检查

- [ ] 论文无主模型口径冲突。
- [ ] 数据、图表、正文结论三者一致。
- [ ] 所有站点满足建设预算和服务半径约束。
- [ ] 所有问题 3 入选定价方案满足 `rho <= 8%`。
- [ ] 紧急救助处理方式前后一致。
- [ ] 问题 4 所有情景均完整重求解。
- [ ] 摘要中的结果数字能在正文表格中找到。
- [ ] 附录代码和输出文件能复现论文结果。
